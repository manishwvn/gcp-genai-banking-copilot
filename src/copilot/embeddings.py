"""Gemini embedding generation (Phase 1 RAG pipeline)."""


def embed_text(text: str) -> list:
    """Generates an embedding vector for text using the Gemini embeddings API.

    Not implemented yet — will call google.generativeai embedding endpoint.
    """
    raise NotImplementedError("embed_text will be implemented in the RAG ingestion phase")
